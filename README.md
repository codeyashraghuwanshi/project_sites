# project_sites
steps to set up GIT

git config --global user.name "project name"
git config --global user.email "email address"

git config --list

make folder of git and open on vs code 

on terminal:

git clone and paste the link(copy https link of project)

cd (foldername ex: projectdemo)
ls
get status

git add (file name) / git add . (here use . means to save all files)

git commit -m 'message'

git push origin main

<br>
Author-Yash
